# Nathan's Projects

Personal site hosting small HTML projects, served via GitHub Pages.

## Adding a project

1. Create a new folder (e.g. `my-cool-project/`) with an `index.html` inside.
2. Add a card linking to it in the root `index.html`.
3. Commit/upload — GitHub Pages updates automatically in a minute or two.
